# RESYIN Publications storefront

React and Vite bookstore. See the repository README for contact details and deployment configuration.

Run `npm install`, then `npm run dev`. Use `npm run build` for a production build and `npm run lint` for code checks.
